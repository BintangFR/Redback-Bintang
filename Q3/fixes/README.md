# Fixes

Place corrected versions of the buggy C++ files here.

Suggested file names:
- `fixed_compile.cpp` – corrected version of `../buggy_compile.cpp`
- `fixed_runtime.cpp` – corrected version of `../buggy_runtime.cpp`

Use GitHub Copilot, a debugger, or your own analysis to identify and apply the fixes.
